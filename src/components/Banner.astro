<section class="mini-banner">
  <div class="container banner-content">
    <div class="banner-badge">
      <span class="dot"></span> Portal Oficial
    </div>
    <div class="banner-text">
      <h2>Tu seguridad es nuestra prioridad</h2>
      <p>Sistema de Acceso UTSJR: Registro rápido y seguro.</p>
    </div>
  </div>
  <!-- Línea separadora institucional -->
  <div class="banner-separator"></div>
</section>

<style>
  .mini-banner {
    background: var(--gradient-header, linear-gradient(135deg, #0D1B2A, #0F4C75));
    color: white;
    padding: 28px 0 0;
    position: relative;
    overflow: hidden;
  }

  /* Patrón decorativo mesh */
  .mini-banner::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 85% 50%, rgba(0, 166, 136, 0.12) 0%, transparent 50%),
      radial-gradient(circle at 15% 50%, rgba(255,255,255,0.03) 0%, transparent 40%),
      radial-gradient(circle at 50% 0%, rgba(0, 201, 164, 0.06) 0%, transparent 60%);
    pointer-events: none;
  }

  .banner-content {
    display: flex;
    align-items: center;
    gap: 30px;
    position: relative;
    z-index: 1;
    padding-bottom: 28px;
  }

  .banner-badge {
    background: rgba(255, 255, 255, 0.06);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    padding: 8px 18px;
    border-radius: var(--radius-full, 50px);
    font-size: 0.75rem;
    font-weight: 600;
    font-family: var(--font-heading, 'Outfit', sans-serif);
    display: flex;
    align-items: center;
    gap: 10px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    white-space: nowrap;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .dot {
    width: 8px;
    height: 8px;
    background: var(--color-accent, #00A688);
    border-radius: 50%;
    box-shadow: 0 0 8px var(--color-accent, #00A688), 0 0 16px rgba(0,166,136,0.3);
    animation: dotPulse 2s ease-in-out infinite;
  }

  @keyframes dotPulse {
    0%, 100% { box-shadow: 0 0 8px var(--color-accent, #00A688), 0 0 16px rgba(0,166,136,0.3); }
    50% { box-shadow: 0 0 12px var(--color-accent, #00A688), 0 0 28px rgba(0,166,136,0.5); }
  }

  .banner-text h2 {
    font-family: var(--font-heading, 'Outfit', sans-serif);
    font-size: 1.4rem;
    font-weight: 700;
    margin: 0;
    line-height: 1.3;
    color: white;
    letter-spacing: -0.01em;
    -webkit-text-fill-color: white;
    background: none;
  }

  .banner-text p {
    margin: 6px 0 0 0;
    opacity: 0.6;
    font-size: 0.9rem;
    color: white;
  }

  /* Línea separadora institucional (firma visual) */
  .banner-separator {
    height: 4px;
    background: linear-gradient(to right, #003057, #00A688, #00C9A4);
    width: 100%;
  }

  @media (max-width: 768px) {
    .banner-content {
      flex-direction: column;
      align-items: flex-start;
      gap: 14px;
    }
    .mini-banner {
      padding: 22px 0 0;
    }
    .banner-text h2 {
      font-size: 1.15rem;
    }
  }
</style>
