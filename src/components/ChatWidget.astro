<div class="chat-container">
  <input type="checkbox" id="chat-toggle" class="chat-checkbox" />

  <label for="chat-toggle" class="chat-button">
    <span class="icon-open">💬</span>
    <span class="icon-close">✕</span>
  </label>

  <aside class="chat-window">
    <div class="chat-header">
      <div class="chat-status"></div>
      <h4>Asistente UTSJR</h4>
    </div>
    <div class="chat-body">
      <div class="message bot">
        ¡Hola! 👋 Soy tu asistente virtual. ¿Tienes dudas sobre tu registro o el acceso al campus?
      </div>
    </div>
    <div class="chat-footer">
      <input type="text" placeholder="Escribe tu duda aquí..." />
      <button>➤</button>
    </div>
  </aside>
</div>

<style>
  .chat-container {
    position: fixed;
    bottom: 25px;
    right: 25px;
    z-index: 5000;
    font-family: var(--font-body, 'DM Sans', sans-serif);
  }

  .chat-checkbox { display: none; }

  /* ── Botón circular ── */
  .chat-button {
    width: 60px;
    height: 60px;
    background: var(--gradient-header, linear-gradient(135deg, #0D1B2A, #0F4C75));
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
    color: white;
    font-size: 24px;
    cursor: pointer;
    box-shadow: 0 6px 24px rgba(15,76,117,0.35);
    transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.3s;
    position: relative;
  }

  .chat-button::before {
    content: '';
    position: absolute;
    inset: -3px;
    border-radius: 50%;
    background: var(--gradient-accent, linear-gradient(135deg, #00A688, #0F4C75));
    z-index: -1;
    opacity: 0;
    transition: opacity 0.3s;
  }

  .chat-button:hover {
    transform: scale(1.08);
    box-shadow: 0 8px 30px rgba(15,76,117,0.4);
  }

  .chat-button:hover::before {
    opacity: 1;
  }

  .icon-close { display: none; }

  /* ── Ventana de Chat ── */
  .chat-window {
    position: absolute;
    bottom: 80px;
    right: 0;
    width: 340px;
    height: 420px;
    background: var(--color-card, white);
    border-radius: var(--radius-lg, 16px);
    box-shadow: var(--shadow-lg);
    display: flex;
    flex-direction: column;
    overflow: hidden;
    border: 1px solid var(--color-border, #e2e8f0);
    
    opacity: 0;
    visibility: hidden;
    transform: translateY(16px) scale(0.95);
    transition: all 0.35s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .chat-checkbox:checked ~ .chat-window {
    opacity: 1;
    visibility: visible;
    transform: translateY(0) scale(1);
  }

  .chat-checkbox:checked ~ .chat-button .icon-open { display: none; }
  .chat-checkbox:checked ~ .chat-button .icon-close { display: block; font-size: 18px; }

  /* ── Chat Header ── */
  .chat-header {
    background: var(--gradient-header, linear-gradient(135deg, #0D1B2A, #0F4C75));
    color: white;
    padding: 16px 18px;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .chat-header h4 {
    font-family: var(--font-heading, 'Outfit', sans-serif);
    font-size: 0.9rem;
    font-weight: 600;
    margin: 0;
    color: white;
    -webkit-text-fill-color: white;
    background: none;
  }

  .chat-status {
    width: 9px;
    height: 9px;
    background: var(--color-accent, #00A688);
    border-radius: 50%;
    box-shadow: 0 0 6px var(--color-accent, #00A688);
    animation: statusPulse 2.5s ease-in-out infinite;
  }

  @keyframes statusPulse {
    0%, 100% { box-shadow: 0 0 6px var(--color-accent, #00A688); }
    50% { box-shadow: 0 0 12px var(--color-accent, #00A688), 0 0 20px rgba(0,166,136,0.3); }
  }

  /* ── Chat Body ── */
  .chat-body {
    flex: 1;
    padding: 16px;
    background: var(--color-bg, #f4f7fa);
    overflow-y: auto;
  }

  .message.bot {
    background: var(--color-card, #ffffff);
    padding: 12px 14px;
    border-radius: var(--radius-md) var(--radius-md) var(--radius-md) 4px;
    font-size: 0.88rem;
    color: var(--color-text, #333);
    max-width: 88%;
    line-height: 1.5;
    box-shadow: var(--shadow-sm);
    border: 1px solid var(--color-border, #e2e8f0);
  }

  /* ── Chat Footer ── */
  .chat-footer {
    padding: 12px;
    display: flex;
    gap: 8px;
    border-top: 1px solid var(--color-border, #e2e8f0);
    background: var(--color-card, white);
  }

  .chat-footer input {
    flex: 1;
    padding: 10px 14px !important;
    border: 2px solid var(--color-border, #d1d9e6) !important;
    border-radius: var(--radius-sm) !important;
    outline: none;
    font-size: 0.88rem;
    font-family: var(--font-body, 'DM Sans', sans-serif);
    background: var(--color-bg-warm, white) !important;
    color: var(--color-text, #333) !important;
    transition: border-color 0.25s;
  }

  .chat-footer input:focus {
    border-color: var(--color-accent, #00A688) !important;
    box-shadow: 0 0 0 3px var(--color-accent-glow, rgba(0,166,136,0.15)) !important;
  }

  .chat-footer button {
    background: var(--gradient-button, linear-gradient(135deg, #00A688, #00C9A4));
    color: white;
    border: none;
    padding: 0 16px;
    border-radius: var(--radius-sm);
    cursor: pointer;
    font-size: 16px;
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .chat-footer button:hover {
    transform: scale(1.05);
    box-shadow: var(--shadow-accent);
  }

  .chat-footer button:active {
    transform: scale(0.97);
  }

  @media (max-width: 400px) {
    .chat-window {
      width: 290px;
      right: -10px;
    }
  }
</style>
