<section class="mini-banner">
  <div class="container banner-content">
    <div class="banner-badge">
      <span class="dot"></span> Portal Oficial
    </div>
    <div class="banner-text">
      <h2>Tu seguridad es nuestra prioridad</h2>
      <p>Sistema de Acceso UTSJR: Registro rápido y seguro.</p>
    </div>
  </div>
</section>

<style>
  .mini-banner {
    background: linear-gradient(135deg, var(--utsjr-blue) 0%, var(--utsjr-blue-dark, #19304f) 60%, #162742 100%);
    color: white;
    padding: 28px 0;
    border-bottom: 3px solid var(--utsjr-green);
    position: relative;
    overflow: hidden;
  }

  /* Patrón sutil de fondo */
  .mini-banner::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 85% 50%, rgba(0, 176, 73, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 15% 50%, rgba(255,255,255,0.03) 0%, transparent 40%);
    pointer-events: none;
  }

  .banner-content {
    display: flex;
    align-items: center;
    gap: 30px;
    position: relative;
    z-index: 1;
  }

  .banner-badge {
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    padding: 8px 18px;
    border-radius: var(--radius-full, 50px);
    font-size: 0.8rem;
    font-weight: 600;
    font-family: 'Montserrat', sans-serif;
    display: flex;
    align-items: center;
    gap: 10px;
    border: 1px solid rgba(255, 255, 255, 0.12);
    white-space: nowrap;
    letter-spacing: 0.03em;
    text-transform: uppercase;
  }

  .dot {
    width: 8px;
    height: 8px;
    background: var(--utsjr-green);
    border-radius: 50%;
    box-shadow: 0 0 8px var(--utsjr-green), 0 0 16px rgba(0,176,73,0.3);
    animation: dotPulse 2s ease-in-out infinite;
  }

  @keyframes dotPulse {
    0%, 100% { box-shadow: 0 0 8px var(--utsjr-green), 0 0 16px rgba(0,176,73,0.3); }
    50% { box-shadow: 0 0 12px var(--utsjr-green), 0 0 24px rgba(0,176,73,0.5); }
  }

  .banner-text h2 {
    font-family: 'Montserrat', sans-serif;
    font-size: 1.4rem;
    font-weight: 700;
    margin: 0;
    line-height: 1.3;
    color: white;
    letter-spacing: -0.01em;
  }

  .banner-text p {
    margin: 6px 0 0 0;
    opacity: 0.7;
    font-size: 0.9rem;
    color: white;
  }

  @media (max-width: 768px) {
    .banner-content {
      flex-direction: column;
      align-items: flex-start;
      gap: 14px;
    }
    .mini-banner {
      padding: 22px 0;
    }
    .banner-text h2 {
      font-size: 1.15rem;
    }
  }
</style>
