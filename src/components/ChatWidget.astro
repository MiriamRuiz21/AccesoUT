<div class="chat-container">
  <input type="checkbox" id="chat-toggle" class="chat-checkbox" />

  <label for="chat-toggle" class="chat-button">
    <span class="icon-open">💬</span>
    <span class="icon-close">✕</span>
  </label>

  <aside class="chat-window">
    <div class="chat-header">
      <div class="chat-status"></div>
      <h4>Asistente UTSJR</h4>
    </div>
    <div class="chat-body">
      <div class="message bot">
        ¡Hola! 👋 Soy tu asistente virtual. ¿Tienes dudas sobre tu registro o el acceso al campus?
      </div>
    </div>
    <div class="chat-footer">
      <input type="text" placeholder="Escribe tu duda aquí..." />
      <button>➤</button>
    </div>
  </aside>
</div>

<style>
  .chat-container {
    position: fixed;
    bottom: 25px;
    right: 25px;
    z-index: 5000;
    font-family: 'DM Sans', sans-serif;
  }

  .chat-checkbox { display: none; }

  /* ── Botón circular ── */
  .chat-button {
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, var(--utsjr-blue), var(--utsjr-blue-dark, #19304f));
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
    color: white;
    font-size: 24px;
    cursor: pointer;
    box-shadow: 0 6px 24px rgba(35,66,108,0.35);
    transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.3s;
    position: relative;
  }

  .chat-button::before {
    content: '';
    position: absolute;
    inset: -3px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--utsjr-green), var(--utsjr-blue));
    z-index: -1;
    opacity: 0;
    transition: opacity 0.3s;
  }

  .chat-button:hover {
    transform: scale(1.08);
    box-shadow: 0 8px 30px rgba(35,66,108,0.4);
  }

  .chat-button:hover::before {
    opacity: 1;
  }

  .icon-close { display: none; }

  /* ── Ventana de Chat ── */
  .chat-window {
    position: absolute;
    bottom: 80px;
    right: 0;
    width: 340px;
    height: 420px;
    background: var(--card-bg, white);
    border-radius: var(--radius-lg, 16px);
    box-shadow: 0 16px 48px rgba(0,0,0,0.15), 0 4px 12px rgba(0,0,0,0.08);
    display: flex;
    flex-direction: column;
    overflow: hidden;
    border: 1px solid var(--divider, #e2e8f0);
    
    opacity: 0;
    visibility: hidden;
    transform: translateY(16px) scale(0.95);
    transition: all 0.35s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .chat-checkbox:checked ~ .chat-window {
    opacity: 1;
    visibility: visible;
    transform: translateY(0) scale(1);
  }

  .chat-checkbox:checked ~ .chat-button .icon-open { display: none; }
  .chat-checkbox:checked ~ .chat-button .icon-close { display: block; font-size: 18px; }

  /* ── Chat Header ── */
  .chat-header {
    background: linear-gradient(135deg, var(--utsjr-blue), var(--utsjr-blue-dark, #19304f));
    color: white;
    padding: 16px 18px;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .chat-header h4 {
    font-family: 'Montserrat', sans-serif;
    font-size: 0.9rem;
    font-weight: 600;
    margin: 0;
    color: white;
  }

  .chat-status {
    width: 9px;
    height: 9px;
    background: var(--utsjr-green);
    border-radius: 50%;
    box-shadow: 0 0 6px var(--utsjr-green);
    animation: statusPulse 2.5s ease-in-out infinite;
  }

  @keyframes statusPulse {
    0%, 100% { box-shadow: 0 0 6px var(--utsjr-green); }
    50% { box-shadow: 0 0 12px var(--utsjr-green), 0 0 20px rgba(0,176,73,0.3); }
  }

  /* ── Chat Body ── */
  .chat-body {
    flex: 1;
    padding: 16px;
    background: var(--bg-color, #f0f4f8);
    overflow-y: auto;
  }

  .message.bot {
    background: var(--card-bg, #ffffff);
    padding: 12px 14px;
    border-radius: var(--radius-md, 12px) var(--radius-md, 12px) var(--radius-md, 12px) 4px;
    font-size: 0.88rem;
    color: var(--text-color, #333);
    max-width: 88%;
    line-height: 1.5;
    box-shadow: 0 1px 4px rgba(0,0,0,0.06);
    border: 1px solid var(--divider, #e2e8f0);
  }

  /* ── Chat Footer ── */
  .chat-footer {
    padding: 12px;
    display: flex;
    gap: 8px;
    border-top: 1px solid var(--divider, #e2e8f0);
    background: var(--card-bg, white);
  }

  .chat-footer input {
    flex: 1;
    padding: 10px 14px;
    border: 1.5px solid var(--input-border, #d1d9e6);
    border-radius: var(--radius-sm, 8px);
    outline: none;
    font-size: 0.88rem;
    font-family: 'DM Sans', sans-serif;
    background: var(--input-bg, white);
    color: var(--text-color, #333);
    transition: border-color 0.25s;
  }

  .chat-footer input:focus {
    border-color: var(--utsjr-green);
  }

  .chat-footer button {
    background: linear-gradient(135deg, var(--utsjr-green), var(--utsjr-green-dark, #008f3b));
    color: white;
    border: none;
    padding: 0 16px;
    border-radius: var(--radius-sm, 8px);
    cursor: pointer;
    font-size: 16px;
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .chat-footer button:hover {
    transform: scale(1.05);
    box-shadow: 0 3px 10px rgba(0,176,73,0.25);
  }

  .chat-footer button:active {
    transform: scale(0.97);
  }

  @media (max-width: 400px) {
    .chat-window {
      width: 290px;
      right: -10px;
    }
  }
</style>
